import { StyleSheet, Text, View, Modal } from "react-native";
import React from "react";

const ResultModal = (skinResult) => {
  return (
    <Modal>
      
    </Modal>
  );
};

export default ResultModal;

const styles = StyleSheet.create({

});
